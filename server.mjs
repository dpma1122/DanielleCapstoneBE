//imports
import express from 'express'; 
import dotenv from 'dotenv'

// setup
const  app = express()
//middleware


//routes

//listener
app.listen(3000, () => {
    console.log('Server Running on PORT:${3000}')
})